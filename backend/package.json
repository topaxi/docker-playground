{
  "name": "backend",
  "description": "Backend for App",
  "version": "0.0.0",
  "scripts": {
    "start": "babel-node bin/app.js",
    "watch": "nodemon --exec npm run start"
  },
  "dependencies": {
    "babel": "^5.1.11"
  },
  "devDependencies": {
    "nodemon": "^1.3.7"
  }
}
